# AGENTS.md

本文件列出 MTP 的硬约束。术语见 `CONTEXT.md`；开发和交接入口见 `Docs/当前决策/`。

## 开发规划与执行入口

- 现行分期开发规划见 [Docs/当前决策/首期开发分层规划.md](Docs/当前决策/首期开发分层规划.md)。开始任何开发任务前，必须先阅读该文件，确认当前期次、目标、范围、依赖和期末卡口；不得按旧规划、聊天片段或个人猜测扩大开发范围。
- 开始实现前还必须阅读 [Docs/当前决策/平台边界与实现议程.md](Docs/当前决策/平台边界与实现议程.md)、[Docs/当前决策/决策落点索引.md](Docs/当前决策/决策落点索引.md) 和 [Docs/设计规格文档.md](Docs/设计规格文档.md)，以现行设计规格、平台边界和已确认决策为实现依据；这些文档与当前票据不一致时，必须暂停并报告，不得自行选择一套规则继续开发。
- 当前期次的执行票据位于 `.scratch/二期开发/issues/` 等对应期次目录。实施前必须阅读当前票据，并只实现该票据定义的一条垂直切片；票据未完成验收前，不得自行开始后续票据。
- 每一期结束后必须进行人工讨论，确认实际产物、架构边界和验收证据；只有讨论通过后，才能创建或领取下一期票据。
- 当实现需要改变现行规划、已确认决策或领域边界时，先暂停编码并更新对应的当前决策或规格，再同步修改调用方、测试和票据；不得在代码中静默形成另一套规则。
- Agent 不得自行进行或宣称完成人工验收。真实 Windows 启动、窗口行为、任务栏嵌入、多屏、DPI、自动隐藏、Explorer 重启等人工验收必须由用户或其他明确指定的人类执行并确认；Agent 只能准备流程、运行自动化验证、整理证据并标记为“待人工验收”，不能用截图、离屏测试、自动化测试或自己的操作代替人工确认。
- 每张票据只要求 Agent 完成除人工验收以外的开发、自动化测试和验收材料；当自动化工作完成并进入人工验收步骤时，Agent 必须自动停止当前 Goal，保持票据为 `ready-for-agent` 或按项目规则标记为“待人工验收”，不得继续操作、勾选人工验收项或将票据标记为 `completed`。只有用户或明确指定的人类返回人工验收结果后，才能由维护者更新票据并决定是否进入下一张票。

## 开发阶段与兼容性

- 本仓库当前是 MTP 的开发环境，不是已发布的稳定版本；当前尚未对外承诺的接口、协议、Schema、数据结构和模块接口可以直接调整。
- 不要为了假设中的旧客户端、旧协议、旧数据或未来版本添加向前兼容层、兼容分支、双写、适配器套适配器或长期迁移代码。优先保持当前实现简单、明确、可测试。
- 只有现行文档已经明确要求的兼容性、协议迁移、稳定 ID 保留、用户偏好保留、旧会话隔离和 MTP 自有状态回退才必须实现；这些要求不能被本条取消。
- 若实现确实需要改变当前契约，先更新对应的当前决策或规格，再同步修改调用方和测试；不要为了保留未发布接口而静默支持多套行为。

## 架构与 Windows 集成

- 平台核心的组件声明、模板积木、布局分配、浮窗类型和降级结果不得依赖 WinUI、Win32、Explorer 句柄或窗口对象；Windows 集成必须经独立适配器接入。
- MTP Host 统一拥有贴靠窗口和 MTP 浮窗生命周期。接入应用只能通过 SDK 或 Broker 的受控声明提供数据、事件通道和动作绑定，不得嵌入任意原生 UI 或窗口。
- 未注册 MTP 协作协议的外部工具不进入布局、事件浮窗或兼容性模型。不得修改、注入、重排、接管或探测其进程以实现协调。
- Explorer 任务栏嵌入是第二阶段实验适配器，不是平台核心或首发前提。失败时必须回退为独立贴靠窗口、隐藏相关组件或报告可解释降级，不能影响其他组件和接入应用业务。
- Explorer 内部窗口类名、窗口树、UI Automation ID 与社区 `SetParent` 代码不是稳定 Windows 扩展契约。未获得真实 Windows 证据前，不得承诺多屏、DPI、自动隐藏、Explorer 重启或第三方工具兼容性。

## 界面、模板与浮窗

- MTP 模板只能组合 Host 提供的受限积木；模板和接入应用不得注入任意代码、窗口、字体、颜色、圆角、阴影或品牌皮肤。
- 所有积木遵守 MTP Fluent 视觉契约，跟随 Windows 深浅主题、强调色和任务栏密度。强调色仅表达已启用、已选中、主要进度、焦点或重要状态，不作为装饰主色。
- 分裂按钮左侧执行主要动作，右侧默认以流畅动画进入同一浮窗组的关联面板。右侧只有显式标记为外部跳转时才能执行其他受控动作。
- 浮窗类型可同时显示。任务栏操作浮窗组锚定任务栏入口；短提示浮窗默认在触发屏幕中央；事件浮窗默认在触发屏幕左下角。高优先级浮窗仅避让或临时遮盖低优先级浮窗，不关闭低优先级浮窗。
- 事件浮窗只能由系统事件或已注册接入应用的事件通道请求；未知本机程序不能直接调用。

## Skill 工作流

- 新功能或方向未收敛：使用 `grilling` 或 `think`；关键事实无法快速确认时使用 `research-brief` / `research`；复杂交互需要体验时使用 `interaction-prototype-to-spec`。
- 已确认结论：使用 `decision-to-spec` 同步正式文档；用户明确授权实施后优先使用 `tdd`。
- 报错、回归、失败测试或行为异常：直接使用 `hunt` 或 `diagnosing-bugs`，修复后重新验证。
- 文档迁移、断链、旧路径或状态一致性：使用 `docs-integrity-audit`；已确认决定没有文档落点时才使用 `decision-to-spec`。
- UI 方向未定时使用 `frontend-design`；用户确认方向并要求实现时使用 `ui`；需要快速验证状态模型或交互方案时使用 `prototype`。
- 单个改动完成后使用 `evidence-based-validation`；阶段或版本验收使用 `phase-acceptance`；合并或发布审查使用 `check`；固定基线双轴审查使用 `code-review`。
- 完整触发条件和人工确认关口见 `Docs/工作流/Skill协作工作流.md`；本文件文字规则优先。

## 测试与验证

- 纯逻辑必须为模板校验、布局分配、冲突降级、浮窗优先级和关闭策略提供自动化测试。
- Windows 集成适配器必须有集成测试；涉及贴靠窗口、浮窗位置、无激活、点击热区、DPI、多屏、自动隐藏或 Explorer 重启的改动，还必须在真实 Windows 环境验证。
- 截图、离屏测试或参考项目源码阅读不能代替真实 Windows 验证。临时探针、原始日志、设备信息和截图不作为现行规格文本。

## 文档与本地状态

- `Docs/` 按材料用途组织：当前决定、调研、工作流和 Agent 约定是当前入口；ADR 与验证反馈按需建立。
- 决策同步默认只更新术语表、唯一主来源和决策落点索引。除非用户明确要求，或报告能新增可执行的证据、验收结果、阻塞、迁移影响或交接信息，否则不得创建“同步报告”“汇总报告”等重复现有正文和索引的文档。
- 调研中的任务书必须保留实际状态。报告未产生或未被采纳前，不得写成完成、已验证或正式决定。
- `.scratch/` 保存本地开发票据和临时规格，不替代现行文档；规则见 `Docs/agents/issue-tracker.md`。
- 不复制 AudioHub 的私有 Docs、`.scratch/`、设备日志或个人信息到本仓库。参考项目的许可证、版权与 SPDX 义务在实际复用前必须单独核对和记录。
- PowerShell 命令用 `;` 连接，不使用 `&&`。
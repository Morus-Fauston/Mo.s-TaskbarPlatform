# Mo's Taskbar Platform

面向 Windows 的协作式任务栏小组件平台。该上下文定义可参与平台协调的界面单元及其与外部工具的边界。

## Language

**接入应用包**:
可包含一个或多个接入应用以及其负责交付的 Windows 原生程序的可分发单元。包是整体安装、更新和移除的边界，不直接拥有 MTP 小组件、浮窗或设置组；包内接入应用分别注册、声明能力，并分别拥有自己的设置组和零个或多个功能组。包内接入应用、功能组和程序随包整体更新，但用户对应用和功能组的启用、禁用与删除状态独立保存；更新包体不自动重新安装或恢复用户此前主动删除的应用或功能组。包可以安装、更新和移除自己负责的原生程序，也可声明是否在删除接入时提供联动卸载该包内程序的选项。包外的外部软件不属于包的安装边界；未声明联动卸载时，删除接入只处理 MTP 注册、运行内容和用户选择的 MTP 偏好，不提示卸载程序。
_Avoid_: 单应用包、MTP 插件包

**自有程序交付型接入应用包**:
由接入应用包交付接入应用自己的包内 Windows 程序的接入形态。包内程序分为包内可直接运行程序和包内需安装程序：前者从包内固定位置直接启动，后者通过包声明的安装方式写入系统或用户安装位置。两者都可由 Host 按包的安装、更新和移除流程管理；开发者负责程序本身的开发、发布、维护和兼容性。
_Avoid_: 把包外软件纳入安装边界、把连接层误称为外部软件本体

**包内可直接运行程序**:
随接入应用包分发、无需单独写入系统安装位置即可启动的 Windows 可执行程序，例如小体量连接层或后台服务。它仍是包内交付物，由 Host 为每个程序分配独立的包内程序目录，并管理其随包的安装、更新、启动和移除；该目录只承载程序文件，不等同于业务数据目录。
_Avoid_: 包外程序、运行中的进程、需要独立安装器的程序

包 ID 是全局唯一稳定身份；应用、功能组、组件、动作槽位和程序使用分层稳定 ID，子级 ID 只需在所属父级内唯一，完整身份由包 ID 和父级 ID 链组成。相同包 ID 的包按同一包处理；同一包内完整身份重复属于声明冲突，不同包可以使用相同的局部子级 ID。

**包内需安装程序**:
随接入应用包分发、需要通过安装步骤写入系统或用户安装位置后才能正常使用的 Windows 程序。它仍属于包的交付边界，由 Host 管理其随包安装、更新和移除，但程序文件使用该程序自己的安装目录；具体安装器和系统注册方式由实现阶段确定。
_Avoid_: 直接运行程序、包外软件、Host 托管业务代码

**MTP 托管进程**:
由接入应用在声明中标记、供 MTP 承担简单运行生命周期管理的包内程序进程。它只适用于职责明确、依赖简单的连接层或运行时服务；Host 可按已声明的启用关系启动、停止、监测心跳并进行有限恢复，不建立通用依赖图。
_Avoid_: 所有包内进程、复杂业务程序、开发者管理进程

首期只支持特别简单的一层依赖：应用可以使用一个自身包内的简单程序，同一包内多个应用可以共享该程序；该程序不能再依赖其他程序。不支持动态依赖、循环依赖、跨包共享或多级依赖。

**开发者管理进程**:
包内由开发者自行负责启动、停止、重启和业务生命周期的程序进程。MTP 只按包流程处理其交付、更新和移除，不监测其存活、不替其编排依赖，也不因其属于包内程序而自动托管。
_Avoid_: MTP 托管进程、Host 通用进程编排器

接入应用包必须在受控声明中列出程序稳定 ID、交付形态、程序路径或安装方式、使用者和进程责任；Host 只按这些声明管理程序，不扫描进程或猜测程序归属。

包内程序的使用关系和依赖由接入应用声明确定。仅被已删除接入应用使用的程序可以随其删除；仍被其他应用使用的程序必须保留。后续包更新新增应用或功能组需要已删除程序作为依赖时，Host 可重新安装该依赖，但不恢复原来已删除的应用或功能组。

只有声明为 MTP 托管进程的简单连接层或运行时服务，才由 MTP 按已声明的简单使用关系准备和运行；Host 不通过扫描进程或猜测文件用途决定依赖。复杂程序、复杂依赖关系和复杂生命周期由开发者自行管理，不进入 MTP 进程编排。未启用应用的独有托管进程不启动；简单共享托管进程在至少一个已启用依赖存在时才运行。

用户启用接入应用或功能组时，MTP 按声明按需准备其依赖程序；准备失败时立即向用户显示具体原因，不将项目伪装成可用。

管理目录和开发者下载源是接入应用的发现、安装和更新渠道，不是已安装应用的运行时依赖。已安装且本地依赖完整的应用可在目录暂时不可用时继续运行。

**接入应用业务数据目录**:
由接入应用在声明中申请、并经 Host 校验通过后由 MTP 提供和隔离的业务数据位置，用于应用自己的配置文件、笔记、数据库、索引和缓存。数据目录按接入应用独立分配；同一包内需要共享数据的程序可在声明中申请包级共享区域。应用可在声明中标记可清理缓存，Host 仅对已声明且实际存在的对象提供清理选项。MTP 管理目录归属、隔离以及应用整体保留或清除选择，但不读取、规定或迁移业务数据格式；数据如何保存、组织、迁移和恢复由开发者负责。程序文件目录与业务数据目录分离。更新默认只替换程序文件并保留已申请的业务数据；删除时按应用整体处理，由用户选择是否保留该应用及其相关内容；未声明或不存在的对象不进入删除询问。
_Avoid_: MTP 统一业务数据库、程序安装目录、跨应用共用默认目录

业务数据的保存、组织、版本迁移、备份和恢复由接入应用负责。MTP 不理解业务数据格式，也不承诺替应用回退已执行的数据迁移。

**外部软件适配型接入应用包**:
由已有外部软件的开发者或适配者提供 MTP 连接层、状态转换和受控声明的接入形态。包可以只包含连接层运行时服务，不负责交付、更新或卸载被适配的外部软件；外部软件通过其明确提供的 API、SDK、扩展协议或用户主动配置的通信入口与连接层协作。连接层可以是包内可直接运行程序，也可以是包内需安装程序，但不因此取得外部软件的安装或卸载权。
_Avoid_: MTP 接管外部软件、注入或探测外部进程、把外部软件本体写成接入应用

包更新后，已安装应用或功能组如果不再被新包声明，Host 停止其运行并标记为当前包已移除或不可用，提示用户决定是否保留或清理相关程序文件、业务数据和 MTP 偏好；不静默清理，也不继续运行未被当前包声明的能力。

新安装的接入应用包完成注册后，应用和功能组默认保持禁用，由用户在首次接入确认中分别选择启用；未启用的应用或功能组不显示组件、浮窗或其他 MTP 能力，也不启动不必要的运行时服务。

包更新发现新增接入应用或功能组时，Host 提示用户并询问是否启用，不静默启用；用户不启用时按已安装但禁用处理，用户之后删除时按已删除处理，不因后续普通包更新重复询问或恢复。只有该项目的声明或能力发生实质变化时，才重新进入确认。

**接入应用**:
通过 MTP SDK 或 Broker 注册、向 Host 提交受控声明的业务单元。一个接入应用归属一个接入应用包，并恰好拥有一个接入应用设置组和零个或多个接入应用功能组；小组件、浮窗和事件通道通过唯一归属的功能组由该接入应用声明。接入应用可拥有一个运行时服务，服务全应用的业务逻辑和功能组共享状态，而非每个功能组各自运行一个进程。
_Avoid_: 接入应用包、MTP 小组件、外部工具

**接入应用运行时服务**:
由接入应用自身运行、通过 SDK 或 Broker 与 MTP Host 受控通信的进程或服务。它负责应用业务逻辑、外部连接、功能组共享状态及声明更新；功能组不直接互相调用。Host 不加载或执行第三方任意业务代码，只协调已登记入口的启动、连接、权限和声明生命周期。接入应用只要存在至少一个已启用且依赖该服务的功能组，Host 就在 MTP 启动时启动该应用唯一的运行时服务，用户可在该应用设置中关闭自启。Host 以 SDK 或 Broker 连接状态和心跳作为服务可用性的主要依据，以已登记进程存在性辅助判断服务是否已退出；进程存在但心跳超时同样视为服务失联。服务可按 Windows 常规安全模型访问其自身业务资源；它经由 MTP 请求系统状态写入、浮窗显示、外部程序启动、MTP 用户设置或屏幕布局信息等能力时，必须经过声明、权限和 Host API。Host 不向它提供任意窗口句柄、其他接入应用数据或任意系统命令执行能力。服务崩溃或断线时，Host 保留最后一次成功状态并标记不可用，禁用依赖该服务的交互控件；对应设置页保留其结构，依赖该服务的实时控件显示“服务未连接”并禁用，静态说明和 Host 托管内容仍可用。恢复优先处理 Broker 和独立连接层，不因通信故障主动重启业务进程；只有业务进程本身失联，或连接层嵌入且声明允许的 MTP 托管进程，才进入相应的有限恢复路径。开发者管理进程不由 Host 自动重启。心跳间隔、超时阈值、自动重启次数与退避时间由实现与真实运行验证确定。
_Avoid_: 每功能组独立进程、Host 托管的任意第三方代码、功能组间消息总线

**连接层进程**:
负责把接入应用业务进程接入 MTP Broker 的包内程序角色。连接层不是独立的生命周期类型，而是与交付形态和运行责任分别建模：交付形态可以是包内可直接运行程序或包内需安装程序，运行责任可以是 MTP 托管或开发者管理。连接层默认服务同一接入应用内的一个业务进程；同一接入应用可按声明使用一对多连接，但不得跨接入应用共享业务会话。Broker 重启或连接层失联时，优先重连或只重启独立连接层，不主动重启仍健康的业务进程。
_Avoid_: 链接进程、跨应用共享会话、默认重启业务进程

**历史快照保留建议**:
接入应用按字段声明某项数据在能力失联或故障时是否适合保留为历史信息的建议。该建议不等于当前状态，也不能覆盖 Host 对实时系统状态、权限状态和安全敏感数据的更严格限制；Host 最终决定是否展示，并必须明确历史或不可用语义。
_Avoid_: 过期状态继续伪装成当前状态、应用单方面决定展示

**Broker**:
由 Host 启动和监督的独立平台进程，是 Host 与接入应用运行时服务之间唯一的受控通信边界。Broker 管理一次服务会话的身份、连接、心跳、消息转发和幂等清理；它转发已校验的声明、状态、动作、浮窗请求和结构化结果，但不解释接入应用业务，不拥有 Host 窗口，也不加载第三方任意代码。Broker 失联时，Host 保留已拥有的窗口、偏好和不依赖外部服务的能力，并显示可解释的平台通信受限状态。
_Avoid_: Host 内部服务、业务逻辑执行器、第三方插件容器、Windows 窗口宿主

**接入应用发现目录**:
由 MTP 维护的接入应用包名单与元数据，首期记录接入应用包 ID、名称、简介、版本、兼容 Host 版本、开发者提供的下载地址和更新源、开发者仓库地址及支持的 Windows 架构，供 Host 发现与更新接入应用。首期是由管理组主动维护的受限管理目录，先收录 Core 以外的指定接入应用，例如 MAH；不开放公众提交。安装包由开发者发布和托管，通常来自其 GitHub Release 或其他官方发布地址，目录不托管或代签安装包。Host 在启动时检查目录，也允许用户手动刷新；网络失败时使用最近一次成功读取的目录并显示暂时无法更新。用户从目录安装时，Host 读取开发者下载地址、下载包、做基础格式和身份校验，再执行安装确认和首次接入确认。发现已安装应用有新版本时，Host 先提示版本更新，用户确认后才下载和安装；新版本新增功能组、权限或外部连接能力时，Host 在更新确认中明确展示变化并取得相应确认。更新过程中的下载、安装、切换或启动检查失败时，Host 尽量恢复更新前的可用版本并显示失败原因；更新期间保留当前版本作为短期恢复副本，待新版本安装成功、进程启动并完成 SDK/Broker 握手后再清理旧版本。更新已成功完成后才发现的业务故障，按服务故障、禁用、撤销或后续版本降级规则处理。Host 在安装或更新前还执行确定性的基础包校验，包括包格式、包和应用身份、版本、声明完整性、稳定 ID 唯一性、依赖指向、程序路径边界、允许的文件范围和 SDK/Broker 协议兼容性；校验失败时不安装或更新并显示具体原因。该基础校验不是恶意代码审核，也不替代后续审核系统。未来生态成熟后可扩展为允许开发者提交的公开社区目录，目录收录仍不构成信任依据；目录只承担发现与更新源，不承担身份信任。
_Avoid_: 信任白名单、官方应用商店、包签名验证

**可信接入应用**:
首期由 MTP 内置可信清单明确标记的接入应用，例如 MAH；后续生态开放后，才评估由管理组白名单和签名验证产生的动态可信身份。可信接入应用可豁免浮窗请求频率限流，但首次接入确认、安装确认和写入权限确认仍照常。被发现目录收录不自动成为可信接入应用。
_Avoid_: 被目录收录即可信、免全部确认、自动授权、把 Core 的内置身份与可安装包混为一谈

**MTP 稳健性**:
MTP 的非功能性要求。任一接入应用运行时服务、Host 托管功能组、组件、浮窗、事件通道或 Windows 适配器发生故障时，Host 必须将影响隔离在对应能力范围内，保留其他应用和 Host 的可用性，提供可解释状态与可恢复入口；不得因单个故障无限重启、遗留不可操作窗口或丢失用户已保存的 Host 管理偏好。具体性能指标、超时和重试参数须经实现与真实 Windows 验证确定。
_Avoid_: 单点故障拖垮平台、无限自动重试、静默故障

**Host 托管声明**:
不需要接入应用运行时服务的受控接入模式。接入应用只提交 MTP 定义的模板、状态字段、可见性规则、设置项和动作声明；Host 仅执行固定且已定义的声明能力，例如渲染状态、按条件显示、更新自身设置、调用已授权系统状态 API，以及读取 Host 提供的系统状态、用户设置、包内静态数据、本地时间日期、网络连通状态和屏幕信息。Host 不加载第三方 DLL、脚本或任意业务代码。复杂业务、外部连接或自定义状态采集必须使用接入应用运行时服务。
_Avoid_: Host 托管第三方代码、脚本插件、任意代码沙箱

**接入应用设置组**:
一个接入应用在 MTP 设置导航中的唯一设置归属。接入应用作者决定声明哪些设置项、分类和二级菜单；Host 只使用受限设置积木统一渲染设置界面，不嵌入任意原生页面、窗口或脚本。Host 校验设置声明是否符合平台界面规则，例如设置项 ID 不重复、积木类型和字段类型受支持、文本和选项数量不过限、操作只指向已声明通道，以及必要的无障碍文本存在；Host 不判断第三方选项在其业务中是否有效。Host 托管功能组直接由 Host 应用其已定义的设置能力。外部运行时服务提供第三方业务设置的选项和值处理，自行校验不可信输入、持久化业务设置并处理应用失败；Host 只将用户通过受控设置积木提交的选择转交给该服务，不解释第三方选项语义或规定其内部恢复和重试实现。涉及 MTP 自身 API 的设置仍由 Host 按既定权限、校验与状态同步规则处理。设置组及其页面不提供接入应用或功能组的启用、禁用和删除操作，这些操作只能在接入应用管理页执行。接入应用声明自身外观时，Host 在该设置页提供“使用应用外观”或“使用 MTP 全局默认”的用户选择；该选择统一作用于该应用全部功能组的小组件和浮窗。用户选择应用外观后，应用更新时其经 Host 校验通过的新外观声明直接生效。禁用单个功能组时，Host 保留该设置组入口，但隐藏该功能组对应的二级菜单、分类和设置项；该应用所有功能组均禁用后，或该接入应用被禁用或删除后，Host 隐藏整个设置组入口及其设置页。
_Avoid_: MTP 浮窗组、屏幕组件组、接入应用功能集合

**接入应用功能组**:
一个接入应用声明的、围绕同一业务功能关联的小组件、任务栏操作浮窗、事件浮窗和事件通道集合。一个接入应用拥有唯一接入应用设置组，并可声明零个或多个接入应用功能组；每个小组件、浮窗和事件通道恰好归属一个功能组，且小组件、浮窗和事件通道各自独立管理。每个可启用的接入应用和功能组都必须至少声明一个可由 MTP Host 托管的入口；只有后台进程、设置或依赖关系而没有 MTP 入口的对象不能单独启用。入口可以按状态隐藏，但不能从声明中缺失。它是用户在接入应用管理页中启用、禁用或删除的对象，不决定设置导航归属，也不表示一次临时浮窗交互。接入应用可将少数用户必须保留的功能组标记为基础功能组；基础功能组不可禁用或删除，业务逻辑仍由接入应用运行时服务负责。
_Avoid_: 接入应用设置组、MTP 浮窗组、屏幕组件组

**Host 托管功能组**:
采用 Host 托管声明运行模式的接入应用功能组。Core 和第三方接入应用均可声明此类功能组；它的状态、可见性、设置与动作只使用 Host 已定义的数据源和固定能力，由 Host 执行而无需接入应用运行时服务；它不包含第三方 DLL、脚本或任意业务代码。需要外部连接、自定义状态采集或复杂业务逻辑的功能组不属于此类。功能组升级为外部运行时模式时，Host 保留同一稳定功能组的用户启用状态、排布、可见性和设置偏好，但首次启动新增运行时服务前必须单独取得用户确认。
_Avoid_: MTP Core、Host 内置功能、外部运行时功能组、Host 托管第三方代码

**接入应用管理页**:
Host 设置中按接入应用分组管理已注册接入应用及其功能组的唯一页面。接入应用和功能组的启用、禁用和删除操作只能在此页执行；接入应用设置组及其页面不提供这些操作。管理页同时展示标记为“官方功能”的 Core 接入应用及其功能组，作为完整注册视图；首期不为这些官方项提供启用、禁用、删除或替代操作。禁用接入应用后，其全部功能组、小组件、浮窗和接入应用设置组入口不可见。若接入应用的所有功能组均禁用或删除，Host 自动将该接入应用标记为禁用。禁用功能组后，该组的小组件和浮窗不可见，组内设置项不在设置导航中显示；若组内仍有已显示小组件，禁用前必须列出将隐藏的小组件和浮窗并请求确认。管理页持续列出已禁用接入应用和功能组，供用户重新启用。删除接入应用或功能组均需要二次确认，并由用户选择保留或清除对应的本地排布、可见性和设置偏好；已删除项立即从管理树移除，只有重新安装或同一稳定 ID 再次声明后才重新出现，并按用户的偏好保留选择恢复或按新功能首次接入处理。
_Avoid_: 小组件排布页、浮窗显示页、接入应用设置组

**设置导航窗格**:
MTP 设置应用左侧的导航区域。它通过导航窗格切换按钮展开或折叠，分为三个区域：顶部固定显示接入应用管理页、小组件排布页、浮窗显示页和官方功能页等 Host 管理入口；中间按接入应用声明顺序显示每个当前可见的第三方接入应用设置组入口，并在超出可用高度时独立滚动；底部固定显示“关于”和“设置”等 Host 固有入口。收起时保留仅图标的窄栏；用户可点击图标切换页面，并通过悬停或点击获得对应名称提示。用户可排序的是小组件排布页中的小组件，不是设置导航入口。
_Avoid_: 接入应用设置组、功能组管理页、页面内二级菜单

**导航窗格切换按钮**:
用于展开或折叠设置导航窗格的三个横线图标按钮，采用 Fluent 的 NavigationView pane toggle 语义。
_Avoid_: 页面菜单、更多操作按钮、功能组操作按钮

**官方功能页**:
设置导航窗格顶部固定的入口，按官方声明顺序列出 MTP Core 中各官方接入应用的设置入口。进入某个官方接入应用后，使用与第三方接入应用相同的设置页；官方功能页及其下级设置页均不提供接入应用或功能组的启用、禁用、删除和替代操作。
_Avoid_: 官方功能组管理页、Core 设置页、合并所有官方设置的单页

**设置页返回按钮**:
始终显示在设置应用顶部，用于按完整访问历史返回上一个仍可达的页面；左侧顶级导航切换同样记入历史。返回时跳过已因接入应用或功能组禁用、删除等原因不可达的历史页面；没有可返回的可达历史页面时保持可见但处于禁用状态。禁用和删除接入应用或功能组只能在接入应用管理页执行。
_Avoid_: 仅在深层页面显示、顶级导航的固定入口、导航窗格切换按钮

**小组件排布页**:
Host 设置中独立管理每个 MTP 小组件的页面。用户可决定小组件是否显示、调整其顺序，并选择所有屏幕使用相同排布或各屏幕独立排布；独立排布时，每个屏幕具有单独的排布区域。用户从统一排布切换至独立排布时，Host 将当前统一排布复制到每个已连接屏幕；从独立排布切回统一排布时，用户选择一块当前屏幕，其排布成为新的统一排布。Host 静默保存每块屏幕的独立排布，之后重新切回独立排布时恢复该历史排布，作为统一排布来源的屏幕仍沿用当前设置。小组件的排布与显示不影响关联浮窗。
_Avoid_: 功能组管理页、浮窗显示页、屏幕组件组

**功能组首次接入确认**:
新第三方接入应用首次安装时，Host 显示总确认页，供用户核对应用身份、来源、权限申请和功能组清单，并选择应用及各功能组的初始启用状态；接入应用声明自身外观时，总确认页还要求用户选择“使用应用外观”或“使用 MTP 全局默认”。未选功能组不得自行显示小组件或浮窗。总确认页只披露第三方接入应用声明的写入权限需求，不在此时授权；已启用功能组首次实际请求某项写入能力时，Host 再单独取得用户确认。用户不启用该应用或不启用任何功能组时，Host 仍完成注册并以禁用状态保留该应用于接入应用管理页，供用户之后重新启用。已启用接入应用在更新时新增功能组后，Host 向用户显示确认入口，由用户选择启用或保持禁用；未确认的新增功能组不得自行显示小组件或浮窗。已安装接入应用在更新时首次新增自身外观声明，Host 同样要求用户选择“使用应用外观”或“使用 MTP 全局默认”。
_Avoid_: 静默默认启用、强制默认禁用

**浮窗显示页**:
Host 设置中独立管理已注册浮窗组是否允许显示的页面。该页面列出接入应用声明的事件浮窗组和短提示浮窗，并分别提供可见性开关；它不管理小组件直接唤起的任务栏操作浮窗组，也不管理浮窗全局默认位置、每屏事件浮窗组上限、材质、不透明度或 Windows 原生浮窗协调策略，这些配置属于全局“设置 > 浮窗”。
_Avoid_: 小组件排布页、功能组管理页、MTP 浮窗组

**全局设置中的浮窗配置**:
全局“设置”页下统一管理 MTP 浮窗的默认位置、每屏事件浮窗组上限、默认材质、不透明度，以及 Windows 原生音量和亮度浮窗协调策略。默认材质和不透明度作用于未声明自身外观的 Core 和第三方 MTP 小组件与浮窗；接入应用可用受控外观声明指定强调色、透明效果和不透明度。它不逐项控制第三方声明的事件浮窗组或短提示浮窗是否允许显示。
_Avoid_: 浮窗显示页、第三方浮窗逐项开关

**MTP 小组件**:
通过 MTP SDK 或协议注册，并可参与 MTP 布局协商的界面单元，例如音乐控制、音频频谱和拾音器状态。
_Avoid_: 第三方任务栏小组件、插件

**外部工具**:
未注册 MTP 协作协议且不受 MTP 进程控制的界面工具。MTP 不探测、不识别、不协调其布局，也不对兼容性作出承诺。
_Avoid_: 未知第三方小组件、冲突组件

**通知区域**:
Windows 任务栏中承载状态图标、时钟等内容的区域，是用户配置 MTP 小组件右侧预留间距时的首选参照区域。
_Avoid_: 托盘展开按钮、托盘三角、上箭头

**屏幕组件组**:
部署在同一屏幕任务栏上的一组连续 MTP 小组件。布局时将该组视为一个整体，其最右侧边缘可应用右侧避让间距。
_Avoid_: 全局组件组、单组件间距

**右侧避让锚点**:
屏幕组件组用于计算右侧预留间距的用户可选参照位置。默认是通知区域左边缘，不可用时回退到任务栏右边缘。
_Avoid_: 固定托盘偏移、三角按钮坐标

**实际避让锚点**:
当前成功用于定位屏幕组件组的右侧避让锚点。平台保留用户的首选锚点，并在发生回退时显示实际锚点及一次性提示。
_Avoid_: 用户首选锚点、固定锚点

**右侧避让距离**:
屏幕组件组右侧边缘到实际避让锚点之间的用户配置距离，以与当前屏幕 DPI 适配的逻辑像素（DIP）计量。
_Avoid_: 物理像素偏移、任务栏长度百分比

**独立贴靠窗口**:
由 MTP 控制的独立顶级窗口，按任务栏和右侧避让锚点定位，但不成为 Explorer 任务栏的子窗口。它是首批交付的承载方式。
_Avoid_: 任务栏嵌入窗口、Explorer 子窗口

**Explorer 任务栏适配器**:
第二阶段的实验性 Windows 集成能力，尝试将 MTP 窗口嵌入 Explorer 任务栏。它不是 MTP 小组件显示或布局协调的前提。
_Avoid_: 平台核心、稳定任务栏 SDK

**MTP Host**:
固定的平台进程，拥有每个屏幕组件组的独立贴靠窗口，并在其中承载和排列 MTP 小组件。Host 提供受控声明模型、模板与 Fluent 渲染、布局和浮窗生命周期、设置壳、权限、系统状态 API、接入应用连接及 Windows 适配器协调；接入应用不直接创建或控制任务栏附近的窗口。Host 不等同于 MTP Core，不承载可替代的业务功能，也不加载第三方任意代码。
_Avoid_: MTP Core、组件应用窗口、分布式窗口宿主、业务功能模组

Host 内部职责按声明与 Schema、能力状态与权限、布局、浮窗调度、包事务、进程监督、Broker 适配和 Windows 适配等模块分开；每类状态只有一个最终负责人，其他模块只能经接口请求，不直接修改别人的状态。包事务和 Broker 适配不得反向依赖具体 UI。

**MTP Core**:
由 MTP 官方随产品提供的内置功能集合，用于实现媒体控制、系统状态等平台自带功能。Core 随 MTP 一起安装并默认注册，不进入接入应用发现目录，不提供单独下载入口；首期官方功能组固定启用，用户不能禁用、删除或替换，只能在其设置页调整具体配置。Core 跟随 MTP 主程序整体更新，不单独更新；更新使用同一次确认和回退，Core 的稳定 ID、声明和数据状态继续保留。Core 的功能声明、设置页、权限记录和 Host 受控 API 与普通接入应用共用同一运行时契约，但生命周期和权限展示入口存在官方例外；Host 不属于 Core 且不可被替换。Core 在 OOBE 中按功能组展示当前版本声明的写入权限，并提供“全部同意”批量入口；用户仍可按功能组或具体能力拒绝，未来新增权限不因既有批量同意而自动授权，之后可全部、按功能组或按能力重新授权。第三方在首次请求相应写入能力时取得确认。Core 更新新增功能组或其他受控能力时，Host 仍向用户提示变化，但不提供禁用 Core 本身的选项。声明模型必须预留同一可替代能力在任一时刻只选择一个当前提供者的扩展条件，但该替代能力不在首期开放。
_Avoid_: MTP Host、不可替换的平台底座、首期已开放的模组市场

**MTP 内置可信清单**:
首期由 MTP 随产品维护的固定接入应用身份清单，用于标记官方认可但不一定随 MTP 安装的接入应用，例如 MAH。清单不等同于公开发现目录，也不开放开发者自助加入；清单中的可安装接入应用仍须经过安装确认、权限确认和正常 SDK/Broker 边界。动态可信白名单、签名验证和审核流程属于后续生态治理议程。
_Avoid_: 目录收录清单、公开审核结果、免确认名单、Core 下载列表

**MTP SDK 开发者签名能力**:
由 MTP SDK 提供给接入应用开发者的正式包签名接口或配套工具，用于对开发者发布的接入应用包进行签名；开发者不需要自行实现签名协议。签名算法、密钥托管、管理组信任根、动态可信白名单和审核流程仍属于后续版本议程。SDK 签名能力不等于目录收录、管理组审核或可信身份。
_Avoid_: 管理目录代签、MTP 统一保管开发者私钥、签名即审核通过、签名即可信

**外部程序联动卸载声明**:
接入应用包作者明确声明的、在用户删除该接入应用时可由 Host 提示用户一并卸载该包负责交付的 Windows 原生程序的受控选项。Host 不根据名称、进程或连接关系猜测程序，也不默认提供该提示；未声明时只删除 MTP 接入，不影响包内程序或边界外外部软件。例如 CodeX 对话提醒助手不负责交付 CodeX，也不声明卸载 CodeX，因此删除助手不提示或执行 CodeX 卸载。
_Avoid_: 删除接入即卸载外部程序、按进程探测决定卸载、替外部软件提供卸载保证

**组件声明**:
接入应用提交给 MTP Host 的组件数据、状态、布局偏好与可执行操作的受控描述；它不包含任意原生 UI 控件或窗口。
_Avoid_: 嵌入控件、组件窗口

**MTP 模板**:
由 MTP Host 使用受限积木渲染小组件的受控视觉与交互定义。模板作者可组合图像、文本、数值和交互按钮等积木，并声明字段与交互槽位；它不包含任意原生 UI、窗口或可执行代码。
_Avoid_: 固定组件类型、应用自绘 UI、任意视图

**模板积木**:
MTP Host 提供并负责渲染、校验的标准界面元素，例如图像、文本、数值、交互按钮和状态标记。模板只能通过组合这些积木定义自身结构。
_Avoid_: 自定义控件、任意 UI 元素

**MTP Fluent 视觉契约**:
所有 MTP 模板积木必须采用由 Host 统一提供的 Fluent 风格，并统一使用用户选择的 MTP 主题模式（跟随系统、浅色或深色）和任务栏密度。模板作者只能声明语义状态，不能覆盖字体、圆角或阴影等基础视觉样式；接入应用可受控声明强调色、透明效果和不透明度，Host 负责校验并使用其支持的效果渲染。
_Avoid_: 模板自定义字体、任意品牌皮肤、应用私有主题、绕过 Host 的视觉渲染

**MTP 主题模式**:
用户在全局“设置”中选择的统一主题模式，可选跟随系统、浅色或深色，并同时作用于 Host、Core 和第三方接入应用的 MTP 小组件、浮窗与设置界面。
_Avoid_: 应用私有主题、仅固定跟随 Windows

**MTP 强调色**:
用户在全局“设置”中选择的默认强调色来源，可选跟随 Windows 系统强调色或自定义 MTP 强调色；默认跟随 Windows。接入应用可在受控外观声明中指定自身强调色。
_Avoid_: 不受控的任意颜色渲染、每个模板随意绘制主题

**强调色语义**:
MTP Fluent 视觉契约中，Windows 强调色仅用于表达已启用、已选中、主要进度、焦点或重要状态，不作为大面积装饰色。
_Avoid_: 装饰主色、模板私有主题色

**动作槽位**:
模板中可由交互按钮或其他手势触发的已命名操作位置。接入应用为动作槽位绑定自身实现，MTP Host 负责请求触发但不执行应用业务逻辑。
_Avoid_: 模板脚本、Host 业务代码

**分裂按钮**:
由并列的主要动作区和关联面板入口区组成的模板积木。主要动作区执行绑定操作，入口区默认以流畅动画切换至同一 MTP 浮窗组中的关联面板；显式标记为外部跳转时，入口区才可绑定其他受控动作。
_Avoid_: 两个无关联普通按钮、无语义箭头按钮

**主激活动作**:
用户点击小组件非按钮区域时触发的可选动作槽位。接入应用可将其绑定为请求 MTP 浮窗、启动已声明的可执行程序或其他受控操作。
_Avoid_: 固定详情点击、默认打开窗口

**MTP 浮窗**:
由 MTP 浮窗框架承载的临时界面。不同浮窗类型可具有不同入口、默认位置、交互能力与关闭策略；不同类型可同时显示，Host 按浮窗优先级避让或临时遮盖，而不关闭低优先级浮窗。所有 MTP 浮窗遵守 Fluent 半透明模糊材质，Host 根据当前环境使用高斯模糊、云母类材质或可解释的视觉降级。Host 统一管理定位、无激活、主题、DPI 和生命周期；首批不支持用户拖动浮窗。
_Avoid_: 应用私有弹窗、组件自建窗口、单一详情浮窗、用户自由拖动、不透明的独立视觉语言

**浮窗关闭策略**:
Host 对 MTP 浮窗组统一执行的关闭规则。任务栏入口浮窗组默认在失焦后关闭；事件入口浮窗组可声明无操作超时后关闭，也可持续显示；短提示浮窗按无操作超时关闭。用户操作重置已启用的空闲计时。
_Avoid_: 全局固定自动关闭、应用自行销毁窗口、把未启用的计时当作必然关闭

**MTP 浮窗组**:
由一次受控入口触发、具有共同上下文和显示状态的一组 MTP 浮窗。浮窗组可包含一个主浮窗和零至多个关联浮窗；关联浮窗可在打开时并列显示，也可由组内动作按需打开。浮窗的展开是 Host 提供的受控动作：模板作者可声明在同一锚点原地扩展为更大浮窗，或打开同组关联浮窗；两种方式都保持同一浮窗组的上下文和状态。关联面板默认以流畅的层级切换呈现，适合并列且空间充足时可并列呈现；Host 在空间不足时回退为层级切换。由任务栏入口触发的浮窗组锚定任务栏入口，不支持用户拖动；由事件触发的浮窗组按其位置策略定位。空间不足时，Host 自动重排窗口；仍无法容纳时折叠低优先级关联浮窗并保留组内打开入口。打开、关闭、失焦和重复触发均作用于整个浮窗组。
_Avoid_: 独立弹窗集合、多个无关浮窗

**短提示浮窗**:
由瞬时状态变化触发的紧凑 MTP 浮窗，例如锁定键状态提示。普通短提示不承载复杂操作；无关联浮窗组时按用户全局默认位置与应用有限偏好显示，默认位置是触发屏幕中央靠下，按无操作超时关闭策略显示，不加入任务栏操作浮窗组。由浮窗组内操作失败触发时作为关联短提示，默认显示在对应浮窗组上方并与该组等宽；上方空间不足时显示在下方，仍不足时由 Host 对该短提示与所属浮窗组降级。普通和关联短提示均以顶层半透明材质覆盖显示，不推动或避让同一屏幕的其他浮窗，且默认指针穿透到下方浮窗。同一浮窗组连续失败时只保留一条关联短提示，更新为最新失败原因并重置关闭计时。
_Avoid_: 通知中心、任务栏操作面板、与触发操作脱离的失败提示、连锁移动无关浮窗、遮挡下方控件输入

**可交互短提示**:
短提示浮窗的可交互子类型，用于音量等需要紧凑即时调节的瞬时状态。它只承载图标、当前读数、即时调节控件和展开入口等必要信息，避免堆叠复杂信息。它使用与其他 MTP 浮窗一致的 Fluent 半透明模糊材质；其中的按钮、滑块等交互控件拦截输入，非交互区域默认指针穿透到下方浮窗。点击展开入口时，使用所属浮窗组声明的展开方式。调用系统状态基础 API 的应用可声明是否显示可交互短提示，未声明时默认不显示；已在 MTP 滑块控件中调节时不额外显示。若应用启用该短提示且请求来自其浮窗组，短提示如关联错误短提示一样浮在所属浮窗组上方。Windows 原生音量和亮度浮窗的 MTP 接管固定显示可交互短提示，不受该默认值影响。按无操作超时关闭，控件操作重置关闭计时。
_Avoid_: 整个提示无条件拦截输入、绕过受限模板积木、任意复杂面板、在不同位置另开无关窗口

**Windows 原生浮窗类别**:
由 Windows 自身显示、可由 MTP 单独配置协调方式的一类原生临时界面。首批类别为音量和亮度调节浮窗；它不包括锁定键等由应用自行创建的提示，也不包括其他应用的窗口、通知或浮窗。
_Avoid_: 未知外部工具、任意第三方窗口、应用自建提示

**原生浮窗协调策略**:
用户为某个 Windows 原生浮窗类别选择的处理方式。在 OOBE 中默认选择由 MTP 接管，并明确说明当前环境可能回退为仅避让：MTP 使用可交互短提示承载该类别的即时调节，并尝试抑制 Windows 原生浮窗；用户可关闭接管，此时 Host 仅避让原生浮窗。首批音量和亮度的接管承载形式固定为可交互短提示，不提供改为事件浮窗组的设置。需要扩展操作时，使用所属浮窗组声明的展开方式。接管不可用、失败或未通过当前环境验证时，Host 必须回退为避让，显示一次短提示，并在设置页保留类别、失败原因、当前协调状态和用户手动重新尝试接管的入口；Host 不自动重试。
_Avoid_: 静默强制接管、修改其他应用窗口、接管失败后阻断系统提示、把承载形式做成无意义设置

**系统状态基础 API**:
由 MTP 提供的、读取和调节 Windows 基础状态的受控 API。首批支持系统音量和亮度的读数订阅、绝对设置和相对增减；MTP 是状态同步的唯一协调者，MTP 自己的组件、浮窗和使用该 API 的接入应用都应反映同一最新状态。只有已注册接入应用可使用；只读能力无需用户授权。调节等写入权限按应用和具体能力分别授予，首次请求时取得一次用户确认，持续有效至用户在设置中撤销；权限声明变化时重新确认。用户拒绝或在设置中撤销某项写入权限后，功能组保持启用并继续提供只读状态，依赖该权限的控件禁用并说明原因；用户首次主动请求禁用控件时只显示原因，五秒内再次主动请求才重新发起该权限申请。权限重新授予后，Host 立即恢复相关控件可用性，但不自动重放授权前被拒绝或未执行的写入动作。写入请求先返回已接收，Windows 实际确认后的最新值和失败结果通过统一状态订阅发布。同一系统状态只保留最新未执行写入请求，后到请求替换旧请求。调用方可声明是否显示可交互短提示。撤销立即禁用该应用当前界面的写入控件，但保留最新只读状态。状态暂时无法读取时，保留最后一次成功读数但标记正在刷新或状态不可用，并禁用相关写入控件；状态恢复后恢复正常显示和可用性。
_Avoid_: 应用直接操作 Windows、各自维护不同的系统状态、未授权写入、撤销后继续调节

**事件浮窗**:
由音量、媒体、设备等系统事件或接入应用声明的第三方事件触发、可包含有限操作的 MTP 浮窗。事件可打开事件入口浮窗组，其主浮窗与关联浮窗共享上下文和状态；位置与关闭策略由 Host 按声明的策略统一执行。同一事件通道重复触发时刷新对应整个浮窗组。
_Avoid_: 系统专属浮窗、任务栏主浮窗、常驻组件窗口

**事件浮窗组**:
由一次事件入口触发的 MTP 浮窗组。一个事件浮窗组包含一个主浮窗和零至多个关联浮窗，是 Host 计算同屏事件并发上限、排列、避让与降级时的基本单位，而不是按组内单个浮窗分别计数。每屏默认最多显示五个事件浮窗组，用户可设置为一至十个；接入应用不得突破该上限。不同事件通道请求新组而达到上限时，Host 关闭最早显示且不受用户操作保护的事件浮窗组后再显示新组；若没有可关闭的组，则不显示新组并返回可解释结果。鼠标悬停、点击、拖动组内控件或键盘操作期间，以及任一此类操作结束后的五秒内，该组受用户操作保护，不能被上限策略替换。浮窗组关闭即结束；同一通道后续事件以最新数据新建浮窗组，不恢复已关闭组的临时界面状态。
_Avoid_: 按组内面板数量计数、接入应用绕过用户上限、虚构事件类别、打断刚操作过的组

**事件通道**:
接入应用通过 MTP SDK 或 Broker 注册的具名事件来源，用于请求事件浮窗。只有已注册应用可声明事件通道、浮窗模板和动作绑定；未知本机程序不能直接请求 MTP 浮窗。
_Avoid_: 任意本地调用、匿名浮窗事件

**浮窗选屏偏好**:
非任务栏入口请求短提示浮窗或事件浮窗时可选的目标屏幕规则。调用方可选择鼠标所在屏幕或指定已知屏幕；未声明时 Host 使用鼠标所在屏幕，无法取得时回退主屏幕。该偏好不包含窗口坐标，最终位置、DPI 适配和避让均由 Host 计算。
_Avoid_: 调用方窗口坐标、应用自行定位

**浮窗位置偏好**:
事件入口浮窗组或无关联短提示在目标屏幕内的有限位置选择。用户设置各自的全局默认位置；接入应用可从 Host 支持的位置中声明偏好，是否允许该偏好覆盖用户默认值由用户控制。无关联短提示默认位置是触发屏幕中央靠下。调用方不得指定任意坐标。
_Avoid_: 应用强制覆盖用户选择、任意坐标定位

**浮窗优先级**:
Host 用于协调不同类型 MTP 浮窗同时显示时的层级与避让顺序。短提示浮窗优先级最高，其次是事件浮窗，任务栏操作浮窗组最低；高优先级浮窗不关闭低优先级浮窗。
_Avoid_: 新浮窗一律替换、全局单浮窗

**外部程序启动声明**:
接入应用为某个动作槽位声明的可执行程序路径、参数、显示名称和唯一标识。MTP 在首次执行该声明前请求用户确认，声明内容变化后必须重新确认。
_Avoid_: 静默启动、任意命令执行